name: bugfix
description: Fix a known bug — skip business analysis, run development through documentation, QA and security in parallel.
match:
  loc_touched_max: 500
phases:
  - development
  - parallel:
      - qa
      - security
  - documentation
