---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [contract]
---

Read this plugin's pipeline-orchestrator skill. When the orchestrator dispatches a phase agent,
what exact value does it pass as `subagent_type`, and what must it never do if that dispatch
returns an error? Answer from the skill, quoting the rule.
