{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "docker compose exec -T app ./vendor/bin/pint || ./vendor/bin/pint || true",
            "description": "Auto-format PHP files with Laravel Pint at the end of any session. Tries Docker first, falls back to local vendor binary, swallows errors so it never blocks the user."
          }
        ]
      }
    ]
  }
}
